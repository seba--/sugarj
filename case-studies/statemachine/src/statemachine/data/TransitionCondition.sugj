package statemachine.data;

import statemachine.Metamodel;
import statemachine.data.EntityExp;

public sugar TransitionCondition {
  context-free syntax
    Event "if" EntityExp -> TransitionFrom {cons("WithCondition")}
}