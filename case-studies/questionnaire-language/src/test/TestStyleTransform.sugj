package test;

import quest.lang.Transform;

public transformation TestStyleTransform {
	main =
	  ?(model, model-path, trans-path);
	  <transform> model;
	  prim("SUGARJ_compile", model-path, trans-path)
	
	rules
	  transform = debug
}
