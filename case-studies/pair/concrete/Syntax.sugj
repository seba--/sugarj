package pair.concrete;

import org.sugarj.languages.Java;

public sugar Syntax {
  context-free syntax
    "(" JavaExpr "," JavaExpr ")" -> 
      JavaExpr {cons("PExpr")}

    "(" JavaType "," JavaType ")" -> 
      JavaType {cons("PType")}
}