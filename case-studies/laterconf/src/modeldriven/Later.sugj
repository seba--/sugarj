package modeldriven; 

public sugar Later {
    context-free syntax
        "$LATER" -> JavaExpr {cons("Later")}
		"$LATER2" -> JavaExpr {cons("Later2")}
		 
	desugarings
	  des-later2
	  
	rules
	  des-later2 : Later2() -> Later()
}   