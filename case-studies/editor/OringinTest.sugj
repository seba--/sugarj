package editor;

import editor.Origin;

public sugar OriginTest {
  context-free syntax
    "abc" "def" JavaID -> ToplevelDeclaration {cons("Text")}

  desugarings
    rule

  rules
    rule : 
      t@Text(s) -> <origin-text> t
}

abc def A
