package config;

import variability.Config;
import model.GraphFeatureModel;

config AllEnabledConfig for GraphFeatureModel {
  enable Cycle;
  enable Undirected;
  enable SearchBase;
  enable Weighted;
  enable StronglyConnected;
  enable Bfs;
  enable GenEdges;
  enable GNOnlyNeighbours;
  enable GNoEdges;
  enable Benchmark;
  enable BaseImpl;
  enable Connected;
  enable Mstkruskal;
  enable Mstprim;
  enable Number;
  enable SearchAlg;
  enable Transpose;
  enable Dfs;
}