package config;

import variability.Config;
import model.GraphFeatureModel;

config AllEnabledConfig for GraphFeatureModel {
  enable CYCLE;
  enable UNDIRECTED;
  enable SEARCHBASE;
  enable WEIGHTED;
  enable STRONGLYCONNECTED;
  enable BFS;
  enable GENEDGES;
  enable GNONLYNEIGHBORS;
  enable GNOEDGES;
  enable BENCHMARK;
  enable BASEIMPL;
  enable CONNECTED;
  enable MSTKRUSKAL;
  enable MSTPRIM;
  enable NUMBER;
  enable SEARCHALG;
  enable TRANSPOSE;
  enable DFS;
}