package config;

import variability.Config;
import model model.GraphFeatureModel;
import model.GraphFeatureModel<variability.CheckConfig>;

public config MinimalGraphConfig for GraphFeatureModel {
   enable Benchmark;
   enable BaseImpl;
   disable SearchAlg;
   enable Undirected;
   disable Weighted; 
   
   disable Number;
   disable Connected;
   disable Transpose;
   disable Mstprim;
   disable Mstkruskal;
   disable Cycle;
   disable StronglyConnected;
   
   disable SearchBase;
   disable Dfs;
   disable Bfs;

   disable GenEdges;
   disable GNOnlyNeighbours;
   enable GNoEdges;
}