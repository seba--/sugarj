package config;

import variability.Config;
import model.GraphFeatureModel;

config MinimalGraphConfig for GraphFeatureModel {
   enable BENCHMARK;
   enable BASEIMPL;
   disable SEARCHALG;
   enable UNDIRECTED;
   disable WEIGHTED; 
   
   disable NUMBER;
   disable CONNECTED;
   disable TRANSPOSE;
   disable MSTPRIM;
   disable MSTKRUSKAL;
   disable CYCLE;
   disable STRONGLYCONNECTED;
   
   disable SEARCHBASE;
   disable DFS;
   disable BFS;

   disable GENEDGES;
   disable GNONLYNEIGHBORS;
   enable GNOEDGES;
}