package impl;

import java.util.Iterator;

model import impl.Graph;
model import impl.Vertex;


public class VertexIter {
  private Iterator iter;
  VertexIter(){
  }
  VertexIter(  Graph g){
    iter=g.vertices.iterator();
  }
  public Vertex next(){
    return (Vertex)iter.next();
  }
  public boolean hasNext(){
    return iter.hasNext();
  }
}
