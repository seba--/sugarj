module Test.Arrow where

import Control.Arrow
import Control.Arrow.Syntax


foo f = proc x -> f -< x+1

bar f = proc x -> f x -<< x+1

baz f1 f2 =
  proc x -> if x == 0
              then f1 -< x+1
              else f2 -<< x+2

exprTail = proc e -> 
   (symbol PLUS -< ()) 
  <+>
   (symbol MINUS -< ())

