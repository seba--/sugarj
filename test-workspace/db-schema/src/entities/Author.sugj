package entities;

import metamodel.Entity;

// import entities.Post;

public entity Author {
  name     :: String
  email    :: Email
  password :: Secret
  posts    -> Set<Post> (inverse=Post.author)
}