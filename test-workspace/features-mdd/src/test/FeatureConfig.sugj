package test;

import variability.Config;

import test.FeatureModel;
import test.FeatureModel<variability.CheckConfig>;

public config FeatureConfig for FeatureModel {  
  enable A;
  disable B;
  enable C;
  enable E;
  disable F;
  disable INTERFACE;
}
