package statemachine.data;

import statemachine.Language;
import statemachine.data.EntityExp;

public sugar TransitionCondition {
  context-free syntax
    Event "if" EntityExp "=>" StateName -> Transition {cons("ConditionalTransition")}
}